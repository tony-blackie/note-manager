import React, { Component } from 'react';
import ReactDOM, { render } from 'react-dom';


export default class NoMatch extends Component {
    render() {
        return (
            <div>404. The page is not available</div>
        );
    }
}